version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14  # or 16, 18
    commands:
      - echo "Installing dependencies"
      
  pre_build:
    commands:
      - echo "Pre-build phase"
      - npm install
      
  build:
    commands:
      - echo "Build phase started"
      - npm run build || echo "No build script found"
      
  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
  name: BuildArtifact
```

#### 3. **If Using AWS CodePipeline with CodeBuild**

When creating your CodeBuild stage in CodePipeline:

**Create Build Project Settings:**
```
Project name: YourProjectName-Build
Environment:
  - Environment image: Managed image
  - Operating system: Amazon Linux 2
  - Runtime(s): Standard
  - Image: aws/codebuild/amazonlinux2-x86_64-standard:4.0
  - Environment type: Linux
  - Compute: Small (3 GB memory, 2 vCPUs) ← KEY SETTING
  - Service role: Create new or use existing

Buildspec:
  - Use a buildspec file
  - Buildspec name: buildspec.yml (or leave blank if in root)
